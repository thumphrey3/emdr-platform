<%= simple_form_for(@session, html: {class: 'form-vertical'}) do |f| %>
	<%= f.association :patient, :label_method => :fullname, :value_method => :id %>
	<%= f.input :appointment_date %> 
	<%= f.input :number, label: 'Session Number', collection: 1..12 %>
	<%= f.button :submit %>
<% end %> 
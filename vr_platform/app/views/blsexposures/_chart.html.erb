<body>
<script type="text/javascript">

	var dataset;

    var w = 500;
    var h = 300;

    var svg = d3.select("body")
        .append("svg")
        .attr("width", w)
        .attr("height", h);

	d3.json("blsexposures.json", function(data) {

		dataset = data["blsexposures"];
		generateVis();
		console.log(dataset)

	});

	var generateVis = function () {

		var xScale = d3.scale.linear()
  						.domain([0, dataset.length])
  						.range([0, w]);

  		var yScale = d3.scale.linear()
  						.domain([0, 10])
  						.range([0, h]);
		svg.selectAll("circle")
   			.data(dataset)
   			.enter()
   			.append("circle") 

   		  	.attr("cx", function(d) {
       		 return xScale(d.run);
   				})
   			.attr("cy", function(d) {
        		return yScale(d.score);
  				})
  			.attr("r", 5);



		};


	// // Adding x-scale and y-scale
	// var xScale = d3.scale.linear().domain([0, lineData.length]).range([padding, w - padding]);
	// var yScale = d3.scale.linear().domain([0, 10]).range([h, 0])

	// //Creating line function to convert data into x and y data points
	// var line = d3.svg.line()

	// 	.x(function(d,i) {
	// 		console.log('Plotting X values for data point')
	// 		return x(d[i].loop);
	// 	})
	// 	.y(function(d) {
	// 		console.log('Plotting Y values for data point')
	// 		return y(d[i].score);
	// 	})

	// 	// Adding an SVG element with desired dimensions and margin
	// 	var svg = d3.select("body")
	// 		.append("svg")
	// 		.attr("width", w)
	// 		.attr("height", h);

	// 	var xAxis = d3.svg.axis()
	// 			  .scale(xScale)
	// 			  .orient("bottom")
	// 			  .ticks(5);

	// 	var yAxis = d3.svg.axis()
	// 			  .scale(yScale)
	// 			  .orient("left")
	// 			  .ticks(5);

	// 	svg.append("g")
	// 		.attr("class", "axis")
	// 		.attr("transform", "translate(0," + (h - padding) + ")")
	// 		.call(xAxis);

	// 	svg.append("g")
	// 		.attr("class", "axis")
	// 		.attr("transform", "translate(" + padding + ",0)")
	// 		.call(yAxis);

</script> 
</body>